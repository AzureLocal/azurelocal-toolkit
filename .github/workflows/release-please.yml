name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  call-shared:
    uses: AzureLocal/.github/.github/workflows/reusable-release-please.yml@main
    secrets: inherit
