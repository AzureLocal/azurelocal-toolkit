# Validation Test Suites

The toolkit includes automated validation scripts in `scripts/validation/`:

!!! warning "Under Construction"
    Test suite documentation is being migrated. See `scripts/validation/` for available test scripts.

## Test Categories

- **Arc** — Azure Arc registration and connectivity validation
- **Cluster** — Cluster health and configuration checks
- **Network** — Network connectivity and SDN validation
- **Storage** — Storage Spaces Direct health and volume checks
